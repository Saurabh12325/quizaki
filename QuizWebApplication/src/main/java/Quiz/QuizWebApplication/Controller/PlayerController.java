package Quiz.QuizWebApplication.Controller;

public class PlayerController {
//    @PostMapping("/login")
//    public ResponseEntity<?> login(@RequestParam String email, @RequestParam String password) {
//        // Define the allowed admin email and password
//        String allowedAdminEmail = "admin@example.com";
//        String allowedAdminPassword = "admin123"; // You can encrypt this for security
//
//        // Check email and password
//        if (email.equals(allowedAdminEmail) && password.equals(allowedAdminPassword)) {
//            // Generate access token
//            String token = tokenService.generateAccessToken(email);
//            return ResponseEntity.ok(token);
//        }
//        return ResponseEntity.badRequest().body("Invalid credentials!");
//    }

}
